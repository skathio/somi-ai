# Example — Reviewer output

> Generated by `/review rate-limiting-webhooks` against the Phase 1 / Iteration 1.1 diff from
> [`feature-plan-example.md`](./feature-plan-example.md): the `RateLimiter` interface and
> `InMemoryLimiter`. Truncated for brevity.
>
> The review file lives at
> `.somi/reviews/rate-limiting-webhooks/2026-05-20-iteration-1-1-approve-with-comments.md`.

---

# Review — `RateLimiter` interface + `InMemoryLimiter` (Phase 1 / Iteration 1.1)

> Reviewer: `reviewer` agent (somi-ai) • Date: 2026-05-20
> Work item: `rate-limiting-webhooks` • Target: `feature-ratelimit-iter1` ↔ `main`

## Summary

This iteration introduces the `RateLimiter` interface and an in-memory implementation, plus a
table-driven test. The shape is good for testability; behavior matches the plan's acceptance criteria.
Two findings — one Major (interface design that pre-empts a decision in Phase 2), one Minor (test
covers boundaries but lacks a true concurrency assertion).

## Verdict

`approve-with-comments`

**Counts**: 0 Blockers, 1 Major, 1 Minor, 1 Nit.

## Findings

### Majors

#### [Major / High] `Allow` signature couples the in-memory impl to a Redis-shaped future

- **Where**: `internal/ratelimit/limiter.go:9-15`
- **What's wrong**: `Allow(ctx context.Context, key string, n int) (bool, time.Duration, error)`
  returns an `error` and a `retryAfter` duration that the in-memory limiter will never actually use.
  The in-memory impl always returns `nil` error and computes `retryAfter` deterministically. Every
  current caller in tests ignores the error.
- **Why it matters**: when the Redis-backed impl lands in Phase 2, it will start returning errors
  that callers have spent two months learning to ignore. The interface should either exercise the
  contract in this iteration or drop it until Phase 2.
- **Suggested fix**: either (a) wire a deliberate logged-and-counted error in the test fake so the
  contract is exercised, or (b) start with `Allow(ctx, key, n) (bool, time.Duration)` and add `error`
  in Phase 2 when there's a real source of error. I'd pick (a) — keeps the interface stable.

### Minors

#### [Minor / Medium] Concurrency test isn't actually concurrent

- **Where**: `internal/ratelimit/inmem_test.go:88-104`
- **What's wrong**: `TestConcurrentAllow` spawns goroutines but synchronises them with a `sync.WaitGroup`
  *after* each one has already finished its work — the goroutines effectively run sequentially.
- **Why it matters**: this test passes whether or not the bucket is actually thread-safe.
- **Suggested fix**: gate the goroutines on a single `sync.Mutex.Lock`-style starting line (or
  `chan struct{}` close) so they fire in a tight burst; then assert the total `Allow`s ≤ budget.

### Nits

#### [Nit / Low] Variable name `n` in `Allow` is opaque

- **Where**: `internal/ratelimit/limiter.go:14`
- **Suggestion**: rename to `cost` or `tokens` so callers don't have to read the godoc to know what
  the `int` means.

## What looks good

- The interface lives in the package root and the impl in a sibling file — clean separation, easy to
  add `RedisLimiter` later without restructuring.
- The clock is injected (`now func() time.Time`), so unit tests don't sleep. Good call.
- Table tests cover the edge cases the plan flagged: burst, expiry, zero-budget. Coverage matches
  the risk register, not "every line."

## Questions for the author

- Why `time.Duration` for `retryAfter` rather than `int` seconds? (Probably fine — `time.Duration`
  is type-safer — just curious if there was a reason vs. matching HTTP `Retry-After: N` directly.)

## Related artifacts

- Spec: `.somi/plans/rate-limiting-webhooks/spec.md` — Phase 1 / Iteration 1.1 maps to `phases/01-limiter-contract.md`.
- Walkthrough of the work-item artifact set: [`feature-plan-example.md`](./feature-plan-example.md).
- Tests run: yes — `go test ./internal/ratelimit/...` (all green).
- Security-reviewer consulted: no (planned for end of Phase 2 per `spec.md` §8).
- Architecture-reviewer consulted: no (no contract changes outside the package).
- Plan-vs-code: diff stayed within iteration 1.1 scope. `progress.md` updated; no diary entry
  needed (finding is a code concern, not a plan concern).
