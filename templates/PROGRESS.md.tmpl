# Progress — <work item name>

> Spec: [`spec.md`](./spec.md) • Phases: [`phases/`](./phases/) • Diary: [`diary.md`](./diary.md)
> Last updated: <YYYY-MM-DD HH:MM>

The **single source of truth for the state** of this work item. Read this first to know where we
are. Updated by `/code`, `/code-loop`, `/review`, `/plan-loop`, `/ship`, and `/ship-loop` as they
progress; can be edited by humans too.

> Status fields appear **only here** — the phase files in `phases/` describe what each iteration
> *is*, not what state it's in. If you find yourself duplicating a status into `phases/`, stop
> and fix that file's template instead.

## Status

<One of: `planning` | `awaiting-approval` | `in-progress` | `blocked` | `under-review` | `done` | `paused` | `abandoned`>

**Started**: <YYYY-MM-DD>
**Target completion**: <YYYY-MM-DD or "open">
**Last activity**: <YYYY-MM-DD — one line on what was done>

## Phase progress

| # | Phase                         | Status          | Iterations done / total | Notes                            |
|---|-------------------------------|-----------------|-------------------------|----------------------------------|
| 1 | [<title>](./phases/01-…md)    | done            | 3/3                     |                                  |
| 2 | [<title>](./phases/02-…md)    | in-progress     | 1/2                     | Iteration 2.2 blocked on D3      |
| 3 | [<title>](./phases/03-…md)    | not-started     | 0/2                     | Awaits phase 2 complete          |

## Iteration progress

Per-iteration status lives here (not in `phases/<NN>-*.md`). Iteration descriptions are over there;
state is here.

| Iteration | Title                            | Status        | Reviewed | Last activity              |
|-----------|----------------------------------|---------------|----------|----------------------------|
| 1.1       | <title from phases/01>           | done          | approve  | <YYYY-MM-DD>               |
| 1.2       | <title>                          | done          | approve  | <YYYY-MM-DD>               |
| 1.3       | <title>                          | done          | approve  | <YYYY-MM-DD>               |
| 2.1       | <title>                          | done          | request-changes → fixed | <YYYY-MM-DD>  |
| 2.2       | <title>                          | blocked       | —        | <YYYY-MM-DD>               |
| 3.1       | <title>                          | not-started   | —        | —                          |

`Status`: `not-started` | `in-progress` | `done` | `blocked` | `superseded`.
`Reviewed`: latest `/review` verdict or `—`.

## Currently in flight

<What's actively being worked on right now. One paragraph or bullet.>

- **Active iteration**: phase <N>.<M> — <title>
- **Owner / driver**: <name>
- **Blocker (if any)**: <description, with link to diary entry or open question>

## Decisions outstanding

Things that still need human input before the next phase can start. Each links to the open question
in `spec.md` §<N> or to a `decisions.md` entry still in `proposed` status.

- [ ] <decision 1 needed> — context in [`spec.md`](./spec.md) / [`decisions.md`](./decisions.md)
- [ ] <decision 2 needed>

## Recent activity

The last few notable events. Full history is in [`diary.md`](./diary.md); this is the recent slice.

- **<YYYY-MM-DD>**: <one-line event>
- **<YYYY-MM-DD>**: <one-line event>
- **<YYYY-MM-DD>**: <one-line event>

## Follow-ups identified

Out-of-scope items discovered during this work item. Filed for the future, not blocking completion.

- [ ] <follow-up 1 — one line, and where it was discovered>
- [ ] <follow-up 2>
